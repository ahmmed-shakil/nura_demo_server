apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "perfomax-be.fullname" . }}-test-connection"
  labels:
    {{- include "perfomax-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "perfomax-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
