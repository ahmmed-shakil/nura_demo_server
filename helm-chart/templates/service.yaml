apiVersion: v1
kind: Service
metadata:
  name: {{ include "perfomax-be.fullname" . }}
  labels:
    {{- include "perfomax-be.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "perfomax-be.selectorLabels" . | nindent 4 }}
